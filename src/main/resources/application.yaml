spring:
  application:
    name: restful
  datasource:
    url: jdbc:postgresql://192.168.50.141:5432/restful
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect